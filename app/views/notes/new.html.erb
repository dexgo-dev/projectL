<h1>New Note for <%= link_to @participant.first_name, participant_path(@participant) %><%= link_to @participant.surname, participant_path(@participant) %></h1>

<%= render 'form', participant: @participant, note: @note, user: @user_id %>
